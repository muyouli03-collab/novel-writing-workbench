# 公开版验证记录

首次验证日期：2026-09-17。以下保留历次记录，最新结论见文末。

## 实际执行结果

| 检查 | 结果 |
|---|---|
| Python 全套回归，首次运行 | 190 项：185 通过、2 失败、1 错误、2 跳过 |
| 修正测试后的定向复验 | 上述 3 个问题用例全部通过 |
| JavaScript 回归 | 61 项全部通过 |
| 独立环境 MCP 协议测试 | 2 项全部通过，含真实 stdio 握手和本地 HTTP 桩 |
| Python 源文件语法检查 | 40 个文件通过（新增测试调整后再次检查） |
| 发布文件检查 | 未复制真实 .env、数据库、模型权重、第三方小说全文及个人投递记录 |

首次 Python 全套中的两项跳过来自主环境未安装 MCP SDK；随后在独立 MCP 环境中执行并通过对应两项测试。初次前端与 MCP 测试受到执行环境创建子进程的限制，允许子进程后复跑通过。

修正的测试问题：

- 提示词目录固定写为 68 项，与现有 72 项不符。保留对运行时代码引用的完整覆盖校验，并按 ID 集合验证接口返回，避免绑定过时数量。
- 原稿追加会话用例已经模拟检索与 LLM，但遗漏凭据校验替身，导致无私人 API Key 时失败。补充该替身；凭据校验行为仍由独立核心测试覆盖。

修正后只复验了这三个失败用例，没有重新运行整个 Python 测试套件；不能将记录描述为“最终版本全套一次通过”。业务源码与前端未修改。

## 测试脚本收录状态

上述测试在公开副本清理前运行。按维护者要求，开发测试脚本与模拟演示服务器已从公开安装版移出，本报告保留历史执行结果。公开仓库当前不能直接复跑这套测试，因此不再提供对应命令。业务功能及 MCP 连接检查工具仍保留。

## 验证边界

使用已有本地虚拟环境运行，Python 3.10.9；没有从零联网安装依赖。本次未验证真实 LLM、Ollama、GPU 精排的端到端效果、性能、成本或浏览器视觉布局。模拟响应测试不替代真实模型效果评测。

敏感信息检查覆盖常见密钥格式、私钥标记、本机用户名与项目路径，未发现匹配项；这是规则扫描，不是完整安全审计。公开版保留 D3 第三方许可证。

后续已补入恢复的 [历史测试报告](测试报告.md)及六次运行的公开指标摘要；本记录仍只描述代码回归验证。本次文档补充未重新调用模型，也未重新运行代码测试。

## 新电脑安装反馈

用户首次双击时缺少项目虚拟环境；建立环境后又因旧 Anaconda Python 不支持 `dict | None` 而启动失败。公开版已补充最低版本与环境检查，并说明使用明确版本重建环境。当时尚未收到成功启动结果；后续维护者已反馈朋友电脑与虚拟机安装成功，见文末更新。


## 初次配置与网页引导验证（2026-09-17）

- 新增配置向导的 16 项隔离测试通过：地址校验、向量协议及无效响应、聊天响应、已有配置保留、跳过与取消、损坏文件不覆盖、DATA_DIR 优先级、旧虚拟环境拒绝、本地服务缺失、本地模型已存在和错误消息不回显密钥。
- 浏览器实际操作通过：自动展示、7 步导航、高亮可见、完成后刷新不重现、手动重新打开、跳过后刷新不重现。使用临时数据目录和独立端口，未上传小说或触发模型推理；页面原有状态检测仍可能查询本机 Ollama 是否在线。
- 配置向导接口测试使用替身，没有实际下载安装 Python、依赖或模型，也没有向远程服务发送请求。新电脑的完整安装和真实模型验证仍需用户执行，未将模拟测试写为安装验收通过。
- 开发测试源码归档到原项目测试结果目录；预览服务、临时数据与本次测试产物在结束时清理。


### 向导结束后自动打开网页

选择暂不配置后，setup.bat 与 start.bat 均继续启动服务；launch_web.py 在服务启动成功后打开浏览器，失败时不打开，浏览器无法打开时提示手动地址。新增 3 项启动器隔离测试通过，原有 16 项配置向导测试通过。本轮未在对方电脑验证系统默认浏览器拉起；测试未调用模型或生成公开版运行数据，临时测试目录自动清理。回归源码保存在工作区“测试结果/公开版回归测试”，不包含在公开安装副本内。


### 2026-09-18 环境准备与网页配置分离

setup.bat 默认仅准备环境，不再进入命令行模型配置。缺少 .venv 时自动创建，缺少依赖时自动联网安装，缺少 .env 时自动复制示例；随后启动网页。在网页设置模型。新增无需输入的首次环境创建及 .env 初始化检查，配置模块共 18 项隔离测试通过，onboarding.js 语法检查通过。用户已反馈手动补齐环境后可正常使用；旧入口为何未完成自动安装尚无错误日志，不能认定已定位。新版真实纯净安装仍待虚拟机验证。本次测试无实际下载或模型请求，临时目录自动清理。


## 本次 GitHub 发布前复核（最新结论）

维护者反馈：已在朋友的实体电脑及 Windows 虚拟机分别验证安装流程，安装 Python 后可以通过 setup.bat 完成环境准备并使用。较早的朋友电脑尝试曾需要手工补装环境。本次反馈未提供完整 Python/Windows 版本及所有功能逐项记录，因此不扩大为所有环境和模型均兼容的结论。

- 隔离副本 Python 回归共 194 项：191 通过，1 失败，2 跳过。失败用例隐含假设 GPU 混合精排默认开启，现已在该用例中显式开启并复验通过；业务默认关闭配置保持不变。未重跑整套，不表述为最终全套一次通过。
- 两项跳过为 MCP 环境测试，随后在独立 MCP 环境完成 2 项复验，全部通过。执行需要允许本地子进程管道，后端为临时 HTTP 桩。
- JavaScript 的 61 项回归通过；索引取消交互脚本另外通过。最初 Node 测试入口被沙箱阻止创建子进程，改用直接运行脚本后通过。
- 环境准备 18 项、浏览器启动器 3 项测试通过。
- HTTP 冒烟检查：首页、前端资源、设置和项目列表响应正常；设置保存后可读回；首页与静态资源均禁止缓存。测试仅使用临时数据与虚构服务地址，未发起模型请求。
- Python 语法、前端 JavaScript 语法、Markdown 本地链接检查通过；常见密钥格式、私钥标记、个人邮箱与本机路径规则扫描未发现匹配。规则扫描不等于完整安全审计。
- 本次修复：启动时实际导入依赖以发现损坏安装；缺少 .env 时重新初始化；首页禁止缓存以减少新旧资源混用；文档统一使用 start.bat 作为常用入口并记录实际安装反馈。
- 本次所有运行数据均在隔离临时目录产生并清理，开发测试源码仍留在工作区“测试结果”，未加入公开包。

发布范围为本机单用户源码安装版，需要安装 Python 并联网获取依赖；并非自带 Python 的离线便携版。本次未重新测量真实模型效果、GPU 性能或费用。原创代码仍未指定开源许可证，保留第三方 D3 许可证。
