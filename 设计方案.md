> 本文保留历史设计与演进记录，包含规划内容；现有入口与安装方法以 README 为准，验证状态见公开版验证记录。

# 仿写辅助 · 设计方案(当前架构 + 演进)

## 2026-08-31：小说创作工作台

产品入口现已扩展为“继续已有小说 / 开始新小说 / 独立范本检索”。新增实现位于 `app/projects.py` 和 `static/workspace.js`；原有检索接口和 `data/index` 范本索引保持兼容。

- 每本本作独立保存 `meta.json`、`project.db`、原文与知识两套向量文件。项目数据库记录章节版本、知识条目、批次确认、检索记录和分析会话的知识快照；全局原稿会话库仍保存完整对话，以兼容旧会话。
- 正文自动保存不调用模型。知识更新按完整章节批次执行；完成批次持久化，待确认批次刷新后恢复。暂停在当前批次完成后生效；服务重启后重新点击更新即可从未完成章节继续。
- 知识可用时间按所有来源章节中最晚的位置判断，避免跨章批次泄露后文。修改、插入、排序或删除章节会使相关后续知识待复核；检索不把已失效索引当作当前事实。
- 模型引用必须能在本批正文定位。用户确认条目优先，参考范本只提供写法，不进入本作知识。模型不能覆盖真实范本书名、原文和章节来源。
- 多范本各自召回候选，再统一混排和精选。向量更新复用文本未变的旧向量；模型切换或索引损坏时允许单独重建向量，不重复调用模型生成已完成的知识。
- 前端保留原有快速、精细、深度和查询优化能力；创作工作台将模式参数放在折叠的高级设置中。

本公开版验证范围与已知限制见 [公开版验证记录.md](公开版验证记录.md)。下文保留原检索系统的设计记录。

> 一句话定位:用户上传一部小说作为「范本」,输入自己的写作场景,程序检索出最匹配的片段,并由 AI 输出**原文片段 + 匹配理由 + 写作技法 + 仿写建议**,帮助用户「学写法」而非「抄文字」。

> 本文档记录**当前已实现架构**、**关键设计决策(含踩坑记录)**、以及**规划中的演进方案**。维护约定:每次改代码同步更新本文档,见文末。

---

## 一、核心原则

**「向量海选 + LLM 精判」,二者各司其职:**

- **向量(bge-m3)只负责"召回"**——快、便宜、可召回率高,但不追求精准。
- **LLM 负责"判断与排序"**——因为向量分高 ≠ 符合自然语言需求(见 §五决策 1)。

这个原则贯穿所有检索模式,是后来逐步演化出"判断模型分离""打分排序交 LLM"等设计的根源。

---

## 二、系统现状(已实现)

### 2.1 两条管线

**管线 A:范本预处理(上传时异步执行)**

```
上传 txt/epub → 解码(txt: utf-8/gb18030;epub: 抽取正文+书名) → 分块 → bge-m3 向量化 → 存索引
                                                              ↑
                                                     后台任务 + 进度条(已实现)
```

- 分块策略:逐行拆分(解决"每段一行"文件)→ 章节识别(行首「第X章」)→ 段落聚合到目标字数(1200)+ 块间重叠(200)+ 硬上限(3000)。
- 索引按作品分目录存储,持久化,重启不丢;多作品可切换。

**管线 B:场景查询**

三种单场景检索模式,详见 §三;前端与原稿对照共同作为四个并列入口,共享结果区和导出能力。

**管线 C:原稿对照**

```
补充信息 + 原稿 → 分析模型总结理解/主动提问 → 用户回答并确认
     → 主动诊断薄弱描写 + 概括前后场景 + 生成丰富目标和检索语句
     → 用户编辑确认 → 选择快速 / 精细 / 深度检索
     → 检索结果追加回同一分析会话 → 范本学习与迁移建议 → 用户继续要求/再次检索
```

原稿会话保存在 `data/draft_sessions.db`,补充信息、模型理解、澄清问题和消息历史均持久化。原稿与补充信息构成固定会话前缀;回答、理解确认、正式诊断和检索结果只追加不回写。确认时选择的检索模式和参数也保存在会话中,后续再次查询会沿用。三种检索模式负责产生最终候选;独立分析模型负责理解原稿、判断哪些描写真正薄弱、概括局部上下文及最终对应判断。
原稿对照完成后在公共结果区展示,导出内容包含原稿、薄弱原因、前后场景、丰富目标、范本片段、对应说明、迁移建议与会话记录。

原稿定位按非空行编号,超长单行按句末切为最多 800 字的片段。模型返回的引用先逐字定位,再尝试忽略空白和中英文引号差异定位;只有不超过 600 字的局部段落才允许作为兜底,相同字符区间只保留一次。批量检索采用分模式限流并发:快速 3、精细 2、深度 1,在缩短等待时间的同时控制外部模型并发量。

诊断结果设有语义字段校验:场景概括需为 30~300 字且不能作为连续引文在原稿中定位,薄弱原因不能与丰富目标相同。旧格式、漏字段或照抄原文会触发同一分析会话内的一次修复调用,利用固定前缀缓存重新生成字段。分析模型按统一优化规则生成完整 `query`,检索层优先直接使用,避免重复拼接；只有用户把它改得过短时才用 `context_summary` 补足语境。

BGE-M3 查询侧默认不加 instruction prefix(`QUERY_INSTRUCTION` 默认为空,仍可用环境变量覆盖以兼容其他模型)。原稿组合查询会压缩为一段自足的自然场景描述,优先保留角色功能、处境、关系/状态变化和写法目标,减少专有名词与重复信息。全书首轮召回仍消费 `/api/embed` 或 OpenAI `/embeddings` 的 dense 单向量;即时结果、快速、精细和深度模式随后统一通过本地 FlagEmbedding 对候选池计算 sparse 与 ColBERT 分数。

统一查询优化采用“显式启用、原文锚定”的双字段设计:`query` 始终保存用户原始写作要求,`search_query` 只保存用户点击按钮后看到并可编辑的优化文本。向量召回与混合精排使用 `search_query`,LLM 判断、精选和深度改写的意图基准继续使用 `query`;未启用时 `search_query` 自动回退为 `query`。原稿对照不额外逐处调用主 LLM,因为原稿分析模型已按同一规则生成 unit.query。

### 2.2 多作品库

每本范本一个子目录(`data/index/<work_id>/`),支持上传/切换/删除,当前作品持久化到 `active.txt`。

---

## 三、检索模式详解

### 3.1 ⚡ 快速检索(默认)

```
场景 → 查询构建 → bge 召回(默认24) → 本地重排(默认8) → LLM 精选(输出段数) + 技法拆解
```

本地重排启用时,快速模式从 dense 排名前 `rerank_k × 3` 的候选中计算混合分并截取 `rerank_k`;精细与深度模式每轮先召回约 `recall_k × 3`（默认上限 200）的 dense 候选,再用 FlagEmbedding `compute_score` 计算 dense/sparse/ColBERT,按 `0.35/0.20/0.45` 合成并筛回 `recall_k`。模型按需加载、单 GPU 串行执行,异常时自动回退 dense 排序。该阶段是候选池混合精排,不是全库 sparse 召回,因此现有作品无需重建索引。

- **查询构建**两种路径:
  - 不点「AI 扩写」= 直接使用完整自然语言场景查询;BGE-M3 官方不要求查询指令前缀,零额外 LLM 成本。
  - 点「AI 扩写」= **HyDE 多版本**(一次生成 3 版假想范文,可选中/可编辑),再检索。
- 适合:场景**具体、明确**。

### 3.2 🎯 精细检索(迭代式,已实现)

```
场景 → bge 召回(默认50) → LLM 分批打分 → 相关片段质心做伪相关反馈 → 再召回 → 多轮(默认2) → LLM 精排(候选池≤12) + 拆解
```

- **打分阶段**:小模型(默认本地 qwen2.5:7b)对每批 5 段按统一 0~3 分量表判断写作参考价值。0=无可迁移内容,1=参考有限,2=有明确可借鉴之处,3=高度贴合。2~3 分进入结果,1 分只作伪相关反馈软种子;不按需求类型或人物身份过滤。
- **伪相关反馈**:`新查询向量 = 0.7 × 相关片段质心 + 0.3 × 原始查询向量`。质心由**强相关(全权重)+ 弱相关(0.4 权重,软种子)**共同构成——弱相关不进最终结果,但让"首轮无强匹配"时检索仍能朝弱相关方向导航,打破鸡生蛋死结(见 §五决策 9)。
- **排序**:分数只做"筛选"和"粗排",最终排序交 LLM 一次性精排(候选池取前 12 段,但只输出用户设定的段数)。
- 适合:场景**抽象、细粒度、技法类**。

### 3.3 🧭 深度迭代(查询改写,已实现)

```
场景 → bge 召回(用改写查询)→ 小模型打分(锚定 q0)→ 精选(锚定 q0)→ 判断/改写 → 再检索,多轮
```

- **两种驱动并存**:
  - **手动**:点「AI 改写查询」生成候选查询 → 用户选/改一个 → 再「检索」,满意即停,可无限迭代。
  - **自动**:点「自动迭代」,主 LLM 做裁判(对比「本轮精选 vs 历史最佳」,锚定 q0);连续"无改进"超过「重试次数」即停(改进会把连败计数归零,故是滑动式循环)。
- **防漂移**:检索用改写查询,但打分/精选/裁判/改写全部锚定 q0 原文;仅用户主动改 q0 时锚点才更新。
- 详见 §六。

---

## 四、AI 端点架构(统一三元组)

四个 AI 组件统一按「**模型 / 地址 / 密钥**」管理,密钥可空(本地部署无需密钥):

| 组件 | 用途 | 默认 | 密钥 |
|---|---|---|---|
| 原稿分析模型 | 原稿理解 + 薄弱描写诊断 + 工具循环 + 对应标注 | 留空=复用主 LLM | 可空/回退主密钥 |
| 主 LLM | 精选 + 扩写 + 拆解 | DeepSeek | 必填 |
| 判断模型 | 相关性打分 | 留空=复用主 LLM;建议本地 qwen2.5:7b | 可空 |
| Embedding | 向量化 | 本地 bge-m3 | 可空 |

两个自动适配(不强制统一):

- 地址以 `/v1` 结尾 → 走 OpenAI 兼容协议;否则走 Ollama 原生 `/api/embed`。
- 地址含 `127.0.0.1/localhost` → 绕过系统代理 + 密钥可空;否则走代理。

实现:持久化在 `data/settings.json`,每次调用实时读取(改设置即时生效,无需重启)。

---

## 五、关键设计决策(含踩坑记录)

这些是从实际测试中得出的结论,是后续设计的**约束条件**:

1. **向量分 ≠ 自然语言相关性**。向量分 58 的段落可能不如 46 的贴切。→ 排序必须交 LLM,向量分只做海选。(实测:向量分 45~49 的片段被 LLM 判为"非常符合")

2. **"送 AI 段数"不是越大越好**。实测从 8 调到 24,结果反而劣化(LLM 面对太多候选会挑花眼、硬凑数)。→ 候选池甜点区 8~12 段。

3. **人物身份不是写法价值的可靠代理**。同一种写法可能出现在主角或配角身上。→ 不按人物身份过滤,只判断片段是否提供可迁移的表达方式。

4. **分批打分跨批不可比**。第 1 批的 3 分 ≠ 第 5 批的 3 分。→ 分数只用于"筛选/粗排",不用于"全局排序";最终排序由 LLM 一次性精排完成。

5. **HyDE 扩写会"加戏"**。纯情绪需求(如"主角困惑")若让 LLM 扩写,它会脑补具体情节(路口/地图/镜子),把检索带偏。→ 纯情绪用查询指令前缀,或扩写时严格约束"只写情绪本身"。

6. **本地模型选择**。qwen3:32b(18.8GB)部分 offload 后太慢,不符合快速查询需求;判断打分这种简单分类任务用 **qwen2.5:7b**(显存和耗时取决于部署环境；本公开版未复测)就够,且免费。

7. **代理陷阱**。httpx 默认 `trust_env=True` 会读系统代理(Clash),把本机 Ollama 请求也转发出去导致 502。→ 本地地址强制 `trust_env=False`,远程地址才走代理。

8. **不同管线的缓存特征不同**。普通迭代检索每次候选不同,前缀缓存收益有限;原稿分析会话则固定“系统提示词 + 完整原稿”并只在末尾追加消息,可充分利用 DeepSeek 前缀缓存。缓存只优化成本,请求仍必须自包含。

9. **精细检索的"鸡生蛋"死结**。伪相关反馈需要先有"相关种子"才能算质心,但首轮打分若全被阈值拒掉(如纯情绪需求,强匹配不独立成段),`confirmed=0` 就直接 break,整个模式空手而归。→ 把打分分三档,弱相关(1~阈值-1)不进结果,但以 0.4 权重进质心作"软种子";只有连弱相关都没有(全 0)才停。

---

## 六、深度迭代(查询改写)方案 —— 已实现

### 6.1 要解决的问题

精细检索(§3.2)的伪相关反馈用**向量质心**,本质是"插值"——只能向已找到的片段靠拢,**不能引入新词、新表达**。短查询/抽象需求(如"主角感到困惑")的召回仍受限。

### 6.2 核心思路

把伪相关反馈从"向量质心"升级为"**LLM 查询改写(文本)**":LLM 对比「精选片段 vs 原始需求 q0」的差异,生成新的查询变体,再检索、打分、对比,多轮迭代。

### 6.3 流程(人工 / 自动双驱动)

```
q0(原始需求) = 全程锚定基准,不可漂移(除非用户主动改 q0)

每轮:
  ① 检索:bge 用"当前查询句"召回候选(检索用改写句)
  ② 粗筛:小模型分批打分(锚定 q0)
  ③ 精选:主 LLM 从候选选「最符合 q0」的片段(锚定 q0)
  ④ 判断是否继续:
     - 手动模式:由用户看结果决定,满意即停(可无限迭代)
     - 自动模式:主 LLM 裁判对比「本轮精选 vs 历史最佳」(锚定 q0)
  ⑤ 改写:主 LLM 对比「精选片段 vs q0」的差异,生成查询变体(锚定 q0)
```

- **手动模式**:点「AI 改写查询」→ 生成 N 个候选 → 用户选/改一个 → 点「检索」下一轮。
- **自动模式**:点「自动迭代」,后台闭环跑;点「停止」随时中断。

### 6.4 三个必须防的坑(成败关键)

| 坑 | 对策 |
|---|---|
| **查询漂移** | 检索用改写句,但打分/精选/裁判/改写都以 **q0** 为基准;仅用户主动改 q0 时锚点才更新 |
| **跨轮评价不可比** | 小模型分数跨轮不可比;自动模式的"变好/没变好"必须用 **主 LLM 裁判**(对比本轮 vs 历史最佳,锚定 q0) |
| **成本爆炸** | 变体数 2~3、召回数收敛、候选跨轮去重、打分走本地小模型、硬性轮数上限 15 |

### 6.5 参数(开放给用户)

| 参数 | 默认 | 说明 |
|---|---|---|
| 召回段数 | 50 | 每轮 bge 召回 |
| 每批片段数 | 5 | 打分每批几段 |
| 输出段数 | 3 | 每轮/最终输出 |
| 判断截断 | 400 | 每段喂判断模型的字数 |
| 改写版本数 | 3 | 手动「AI 改写查询」生成的候选数 |
| 无更好结果后重试次数 | 1 | 自动模式:连续"无改进"几轮后停(0=立即停) |

> **重试次数是"滑动式连败计数",不是全局上限**:每次"变好"都把连败计数归零,重新获得"重试次数"额度。因此总轮数 ≈ 改进次数 × (重试次数+1),消耗非线性。

### 6.6 与现有模式的关系

作为**第三种模式「深度迭代」**,与快速/精细并存,不替换。精细检索适合大多数需求;深度迭代留给"精细检索仍不满意"的难需求。

---

## 七、演进路线

- [x] M1 片段先行:txt 上传 → 分块 → bge-m3 检索 → 返回片段(零 LLM)
- [x] M2 API 精读:LLM 精排 + 技法拆解 + 仿写建议
- [x] M3 质量增强:多作品库、HyDE 扩写多版本、精细检索(迭代式)、判断模型分离、统一参考价值评分、AI 端点统一管理、进度条
- [x] M4 深度迭代:LLM 查询改写 + 裁判闭环,人工/自动双驱动(§六)
- [x] M5 原稿对照:持久化分析会话、薄弱描写诊断确认、上下文检索工具循环、范本学习建议与再次查询
- [ ] M6(可选):reranker 粗筛前置层(大规模召回时)、流式输出(SSE)、结果缓存

---

## 八、知识整理记忆与维护工具（2026-08）

- 项目数据库新增 `knowledge_workspace_state`，按项目保存整理会话、设定建议、合并草稿/历史和查漏检查点；知识批次增加 `context_info` 用于记录来源、续接原因与真实用量。旧项目通过增量建表/加列兼容。
- 建库上下文由相关历史知识、核心设定和近期剧情组成，使用现有知识向量辅助排序；无向量服务时回退关键词。严格限制来源早于本批首章。稳定系统消息及原始历史消息不改写，新批次和确认反馈追加；6小时或60000字符安全阈值等条件触发新会话，而不是压缩改写旧前缀。
- 从知识提炼的初始设定记录来源条目指纹、章节版本和最大来源位置；确认前不写项目，已有字段须明确允许替换。调用模型前排除失效或超出当前章节位置的派生设定，手工独立输入仍按作者确认资料处理。
- 合并采用“草稿→用户编辑确认→事务插入新卡片并归档原卡片”。`knowledge_items.active=2` 代表合并来源，不混入删除回收站。默认列表只见合并卡片；带章节边界的历史检索在合并尚不可见时可返回归档原卡片，向量索引保留这部分历史记录。原卡片及全部引文可恢复，正文不变。
- 查漏以完整章节分批，直接来源于本批的所有现有卡片加相关知识共同对照。不丢弃正文尾部；超长章节或过量知识会明确暂停，不伪报已检查。每批先校验原文引文再保存完成检查点，疑似遗漏需用户确认才能以追加方式写入。
- 查漏引文采用逐候选、逐引文校验：先逐字匹配，再仅忽略空白并统一中英文引号进行定位；成功后始终保存小说正文中的原始子串。某候选只要保留一条有效引文即可进入报告，完全无有效依据的候选写入 `discarded_findings` 并跳过，不再使整批回滚或反复调用模型。失败候选保留标题、模型引文和章节范围供界面诊断，最多保存最近200条。
- `audit/start` 支持可空 `chapter_ids` 与可选 `audit_id`。显式空数组、空章及其他项目的章节均拒绝，不回退全文。按小说顺序对所选完整章节分组，报告持久保存 `scope` 与 `selected_chapter_ids`；续查/重查通过报告ID绑定原范围，不能混入新的章节勾选。旧报告缺少范围字段时按全文兼容。前端使用独立的 `audit_chapter_selection` 能力标记，阻止旧后端忽略范围字段后误查全文。
- 查漏记录使用正文版本与知识指纹检测外部变化，暂停/重启可继续未完成批次；仅用户从当前报告补入的知识允许更新签名并接续。合并、设定提炼、建库及查漏在同一项目内互斥，避免同时产生互相矛盾的报告。
- 新接口在 `/api/projects/{id}/knowledge/` 下：`session/reset`、`settings/suggest|apply|dismiss`、`merge/suggest|apply|undo|dismiss`、`audit/start|stop|review`。生成动作后台运行并沿用进度接口，确认写入与向量更新失败区分反馈。

## 九、自动合并与提示词方案（2026-08-31）

- 自动合并复用现有草稿与事务合并机制。`POST /api/projects/{id}/knowledge/merge/auto` 必须接收用户明确勾选的 `chapter_ids`，并接受 `max_groups`（1～20，默认5）与 `apply_automatically`（默认false）。后端按完整章节分批，每一批先发送所选章节全文，再扫描全部有效知识卡片目录，识别重复、阶段变化及后文已解决的早期不确定判断，随后逐组生成合并草稿；相同章节前缀也便于兼容模型服务利用前缀缓存。`merge/stop` 请求在当前模型调用后停止。默认不修改有效卡片；显式自动应用时新卡片状态为auto，来源归档及撤销逻辑保持不变。剧情索引不参与卡片合并。
- 知识建立、自动合并和查漏共用模型感知的单章安全上限。设置项 `analysis_context_tokens` 可留空自动识别：`deepseek-v4-*`/`[1m]` 按100万 token，未知兼容模型仍保守为3万字；显式填写时正文预算取上下文的30%，最低3万、最高30万字。其余上下文留给卡片目录、关联知识、系统提示词、思考和输出。原稿分析会话的3万字限制属于另一条流程，本次不联动。
- `reading_session` 作为项目本地状态保存合并/查漏共用的稳定消息前缀、已读章节和版本指纹。任务只在前缀末尾追加本次范围与卡片/知识，不把每次任务响应写回固定前缀，从而避免上下文无限膨胀并提高跨工具前缀缓存复用。连续建库会话具备明确 `read_chapter_ids` 时可作为首次阅读前缀；缺章按末尾追加，超过正文预算则新建。任何正文、模型或提示词变化都会使旧阅读会话失效。
- 有名称和方向的卡片关系存入原有 `details.knowledge_relations=[{target_id,label}]`，无需迁移数据库；旧 `related_knowledge_ids` 继续按“相关”读取，首次编辑涉及该卡片的关系时转换。`PUT /api/projects/{id}/knowledge/{item_id}/relations` 接受 `{related_id,label,direction}`，验证同项目、有效状态、关系名、方向、去重及50条上限，并在事务中重建该卡片的全部入边/出边；项目知识接口为两端返回轻量、有方向的关系图。失效ID不会进入当前关系图或模型依据。
- 合并相关上下文先纳入手动关联，再按所选卡片与其他卡片之间的标题、别名、人物字段及正文摘要互相提及排序；通用称谓不作为关联。最多12张、12000字符，作为独立的只读用户消息发送，与合并输入严格分区。草稿持久保存关联快照，确认时同时校验所选与只读卡片指纹/章节版本；新卡片仅保存关联ID，不复制关联卡片来源或正文。
- 候选目录从当前项目有效知识读取，排除已被待确认草稿占用的卡片。按类型、标题及别名排序，每批最多40张、约24000字符，重叠6张；每条摘要最多1200字。分组须完全属于输入集合、每组2～12张、组间不复用卡片。识别和生成前后均校验来源指纹，正文/知识改变则终止；已经保存的草稿不会因后续网络失败而丢失。
- 知识接口返回 `auto_merge_supported` 与 `merge_proposals`，前端下拉切换全部待确认草稿，兼容旧单草稿接口。索引更新结束后再发布最终完成状态，避免前端重复恢复已经完成的任务。自动扫描不是全库语义聚类保证，跨批关联仍可用手动合并补充。
- `app/prompt_defaults.json` 是54个内置模板的单一来源；`app/prompts.py` 负责目录、纯文本变量替换、方案校验和本地持久化。LLM构建消息、原稿后续指令和知识确认反馈全部读取模板，不只是在旧指令后追加自定义文本。
- 新增 `GET /api/prompts`、`POST /api/prompts/presets`、`POST /api/prompts/presets/{id}/activate`、`DELETE /api/prompts/presets/{id}`。保存方案只保存相对内置模板的覆盖值，并启用该方案；原子替换写入 `data/prompt_presets.json`，内置方案只读，单模板最多20000字，最多100个方案。用户正文和运行变量不会写入方案。
- 模板变量集合必须与内置模板一致，替换只执行一次，不解析代码、不展开正文里的花括号。用途和变量说明在设置页展示，JSON结构由用户保留，既有响应解析及来源验证继续生效。
- API请求通过ContextVar冻结提示词版本，后台任务继承该版本，切换方案不会把同一运行任务的前后调用混用。知识整理会话的身份指纹加入提示词覆盖内容摘要；既有原稿消息按原样保留以避免破坏缓存，需新建分析会话才能换系统前缀。
- 新增离线测试覆盖模板调用点目录一致性、实际模型消息注入、方案保存切换、变量验证、上下文冻结、自动合并预览/直接保存/撤销、失败保留及非法分组拒绝；所有模型均为模拟响应，不使用用户项目。

## 十、剧情索引与实体知识分离（2026-09-01）

- `plot` 的存储类型继续保留，以兼容既有项目、向量文件、分析快照和提示词方案；对外语义改为按章节排列的“剧情事件索引”，而非可反复覆盖合并的实体卡片。无需迁移数据库或重建已有小说。
- `GET /api/projects/{id}/knowledge` 将有效 `plot` 条目放入 `plot_index`，普通 `items` 与 `knowledge_catalog` 排除剧情。索引条目增加由逐字引文反查得到的 `index_chapter_ids/index_chapters`；旧批次把多章都记为来源时，也优先显示真正含引文的章节。
- 剧情索引继续进入本作向量索引、章节边界检索、建库历史上下文和查漏去重，因此不会削弱前情查询。前端按章节顺序展示事件、引文和可展开原文，不提供卡片合并或关系管理控件。
- 卡片关系、自动合并、手动合并的候选集合均排除 `plot`。后端同时拒绝绕过页面提交剧情合并或剧情关系，防止接口调用重新制造“剧情卡片”。人物、关系、名词、世界观、场景和伏笔保持原有卡片能力。
- 内置知识整理提示明确要求每个 `plot` 只记录一个可定位事件，包含参与者、条件、行动和明确结果，不把跨阶段事件压成总档案。逐步确认仍可选择剧情类型，保存后自动进入索引。

## 十一、维护约定

**每次修改代码,同步更新文档**,让没看过代码的人也能跟上思路:

- 功能/参数/交互变化 → 改 `使用指南.md`
- 架构/设计决策/演进方案 → 改本文档
- 顶层入口/运行方式 → 改 `README.md`
# 细分标签时段

保留 `knowledge_items.details.tags` 的字符串数组和原有类型标签目录，新增同一详情内的 `tag_periods` 数组，每项包含 `tag`、`valid_from_chapter_id`、`invalid_from_chapter_id`。同一标签可有多个时段；未设置时按不限定时间兼容，无需迁移已有数据库。

更新卡片时在同一数据库连接中校验章节存在性和起止顺序，避免再次出现写事务内第二连接读库的问题。读取时附加可读的章节标题及时间状态；按章节读取时以所选章节为判断界限。原始时段仍保存在详情中，删除或重排章节不会静默清除历史。

普通列表与画布共用 `static/tag_periods.js` 编辑和展示。合并以原卡片人工时段为准，保留同标签不同区间，沿用原有合并撤销快照；同名AI补充保护时段字段。知识上下文和MCP卡片摘要同时携带时间范围，避免只传标签名称造成时态误读。


## 公开版首次使用流程

- `setup_wizard.py` 仅使用标准库，应用依赖尚未安装时也能运行；先检查解释器，再创建虚拟环境和安装基础依赖，所有联网安装与接口测试均由用户选择。
- 初次配置将模型设置原子写入数据目录的 `settings.json`，保留无关设置；已有文件损坏时停止覆盖。`setup-state.json` 记录向导已访问，不表示模型可用。`.env` 仅在缺失时从示例复制。
- `start.bat` 在缺少环境、依赖或初次访问记录时进入配置流程，正常情况下直接启动。已有旧解释器不会自动删除或覆盖，提示按 README 重建。
- `static/onboarding.js` 独立实现 7 步非模态页面引导，使用现有导航与 CSS 高亮，支持返回、跳过、Esc 和重新打开。浏览器本地存储记录完成/跳过；存储不可用时不自动弹出，仍可手动打开。
- 脚本向导负责配置与接口检查，网页引导负责解释入口。未新增模型自动调用、自动导入或自动知识生成。
