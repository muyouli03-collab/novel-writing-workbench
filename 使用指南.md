# 使用指南

## 第一次使用

双击 `start.bat`；缺少环境时自动调用 `setup.bat`，自动检查 Python、创建虚拟环境、联网安装基础依赖并初始化缺少的 `.env`；完成后直接打开网页。在网页“设置”中配置本地或远程模型，也可以先体验正文编辑。命令行不再要求选择模型或输入密钥。

网页第一次访问会自动展示入门引导。可以点击“上一步”“下一步”，点击“暂时跳过”或按 Esc 退出；以后从顶部“入门引导”重新打开。完成状态按浏览器与站点地址保存，换浏览器或端口可能重新出现。引导不会替你填写、保存配置或调用模型。

环境准备保留已有设置，模型选项仅在网页点击“保存设置”后写入。更换向量服务或模型后，需要重建已有向量索引。首次配置记录存入数据目录的 `setup-state.json`，它只表示已完成环境准备，不表示模型接口验证成功。

## 三个入口

- 继续已有小说：导入 TXT / EPUB，管理章节，再按需建立知识库。
- 开始新小说：创建项目并编辑正文；正文自动保存，知识生成需要主动触发。
- 独立范本检索：导入参考文本并建立索引，输入写作需求检索片段。

演示素材与可核对的步骤见 [examples/README.md](examples/README.md)。

## 模型设置

在页面设置中配置 Embedding、主 LLM、判断模型与原稿分析模型。分析模型和判断模型留空时可复用主模型，具体以页面提示为准。使用本地模型时可留空密钥；远程服务需填写自己的凭据。

示例 `.env` 默认关闭 GPU 混合精排。基础向量检索需要先启动 Ollama 并拉取 bge-m3。设置保存在 `data/settings.json`，优先于 `.env`；排查配置问题时检查页面中实际生效的值。

## 检索模式

快速模式用于单次检索与精选；精细模式通过分批判断和反馈扩大检索；深度模式增加查询改写与迭代。先用快速模式，再根据实际结果决定是否增加调用。原稿对照可先诊断需要补充的描写，再查询范本。

AI 查询优化由用户主动启用，优化结果可以修改；最终判断仍以原始要求为基准。结果保留来源，范本内容不应作为本作人物或剧情事实。

## 知识库与画布

选中需要处理的章节再建立知识库。人工核对 AI 条目和原文依据，按需确认、编辑、合并或添加关系。修改正文后检查相关知识是否需要更新；更换 Embedding 模型后可重建向量索引。

MCP 的章节范围需要每次查询传递，不能视为永久限制。具体见 [MCP 使用说明](MCP使用说明.md)。

## 可选 GPU 混合精排

1. 安装适配本机 NVIDIA 驱动与 Python 环境的 CUDA 版 PyTorch。
2. 在项目虚拟环境安装 `requirements-hybrid.txt`。
3. 将 BGE-M3 官方权重放到 `data/models/bge-m3`（包含 `pytorch_model.bin`、分词器和 sparse / ColBERT 权重等完整模型文件），或用 `BGE_HYBRID_MODEL_DIR` 指定路径。
4. 在 `.env` 设置 `BGE_HYBRID_ENABLED=1`，重启后检查运行状态。

相关来源：[PyTorch 安装入口](https://pytorch.org/get-started/locally/)、[BGE-M3 模型](https://huggingface.co/BAAI/bge-m3)。这些是安装参考入口，本次整理未联网验证依赖安装与模型下载。

当前实现要求可用 CUDA，不在 CPU 上运行混合精排；不可用时回退 dense 检索。关闭该项不影响基础页面与数据管理。

## 数据与限制

数据保存在项目的 `data/` 下；备份时妥善保管该目录，公开仓库不包含它。运行日志和本机密钥也不应提交。调用云端 LLM 会将相关输入发送到所配置的服务。

历史案例见 [测试报告](测试报告.md)，其中耗时来自旧记录，不能作为当前版本的性能承诺；当前回归验证见 [公开版验证记录](公开版验证记录.md)。

## 启动故障

双击启动前需创建 Python 3.10+ 虚拟环境并安装依赖。若停在 `dict | None` 并报类型不支持 `|`，说明实际使用了旧解释器，应重建环境而不是改业务代码；详见 [README 的安装与旧环境说明](README.md)。启动脚本现会先检查环境与版本，再在当前窗口显示服务日志。
