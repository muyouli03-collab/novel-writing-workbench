# 三模式检索测试报告

> 历史测试：2026 年 8 月；报告恢复：2026-09-17。
> 本报告依据本地保存的原始 JSON、测试脚本和旧对比记录整理，本次未重新调用模型或运行在线检索。结果反映当时版本，不代表当前版本的性能验收。
> 公开版提供[六次历史运行的指标摘要](evaluation/historical-metrics.json)，不包含第三方小说正文、召回候选原文和旧导出文件。下文提到的本地原始材料未随仓库发布，因此公开摘要不能用于独立复核片段质量或完整复现历史测试。

## 1. 测试目的与证据范围

比较快速检索、精细检索和深度迭代在同一情绪描写需求下的输出数量、迭代过程及参考价值，并观察场景化查询对精细检索的影响。

数据口径：

- **可核验数据**：JSON 中的完成状态、实际轮数、确认段数、输出段数、章节、评分及深度迭代历史。
- **历史记录数据**：环境、耗时、额外重复运行和调用次数来自旧对比记录。现存 JSON 没有完整计时、模型配置快照或逐次请求账单，不能独立验证这些数据。
- **评价边界**：这是单部范本、单个主要需求的案例测试，没有人工标注集；输出数量和模型评分不等于准确率或人工质量评分。

## 2. 测试环境与参数

旧记录记载的环境如下，模型名称按记录保留：

| 项目 | 历史配置 |
|---|---|
| 范本 | 《神秘复苏》，5063 个分块 |
| 主 LLM | `deepseek-v4-flash` |
| 默认判断模型 | 本地 Ollama `qwen2.5:7b` |
| Embedding | 本地 Ollama `bge-m3` |
| 服务 | `http://127.0.0.1:8000`，FastAPI |
| 客户端 | Python + `httpx` |

原始需求见 `query.json`（本地原始材料）：

> 主角深夜独自收到一条坏消息，愣在原地，想哭却哭不出来，只能反复深呼吸强压情绪

请求参数由 `run_modes.py`（本地原始材料）核验：

| 模式 | `recall_k` | `rerank_k` | `rounds` | `batch_size` | `top_k` | `judge_chars` | 其他参数 |
|---|---:|---:|---:|---:|---:|---:|---|
| 快速 | 24 | 8 | — | — | 3 | — | `expanded=""`，不扩写 |
| 精细 | 60 | — | 2 | 5 | 3 | 400 | 历史参数 `category="emotion"` |
| 深度 | 60 | — | 自动 | 5 | 3 | 400 | `category="emotion"`、`variants=3`、`retry_count=1/2` |

精细模式还记录了切换至主 LLM 判断的对照运行。脚本通过暂时清空判断模型和地址实现回退，文件名为 `refine_ds.json`；其具体模型依赖当时服务设置，JSON 本身没有保存模型名称。

## 3. 实测结果总览

下表每行对应一份现存结果文件。耗时引用旧记录，未作为本次重新测量值。

| 测试 | 实际轮数 | 确认段数 | 输出段数 | 历史耗时 | 本地结果文件（公开指标见摘要） |
|---|---:|---:|---:|---|---|
| 快速，不扩写 | — | — | 3 | 31.3 秒 | `fast.json`（本地原始材料） |
| 精细，本地判断 | 1 | 0 | 0 | 约 12 秒 | `refine_local.json`（本地原始材料） |
| 精细，主 LLM 判断 | 1 | 0 | 0 | 约 60 秒 | `refine_ds.json`（本地原始材料） |
| 深度，`retry_count=1`，保留的空结果样例 | 2 | 0 | 0 | 旧记录中空结果为约 27～30 秒，无法确定本文件的精确耗时 | `deep.json`（本地原始材料） |
| 深度，`retry_count=2` | 5 | 10 | 3 | 约 205 秒 | `deep_retry2.json`（本地原始材料） |
| 精细，场景化需求、本地判断 | 2 | 3 | 3 | 72.4 秒 | `refine_scene.json`（本地原始材料） |

所有异步结果文件的 `status` 均为 `done`。精细模式配置两轮而只运行一轮，是历史流程提前停止的结果；不能把空结果直接判定为接口运行失败。

旧记录另称 `retry_count=1` 共运行三次，一次输出三段、两次输出零段；目前只保留了一份空结果 JSON，另外两次仅能作为文字记录引用，不据此计算可复核的成功率。

## 4. 各模式结果分析

### 4.1 快速检索

快速模式返回三段，并带有匹配理由、技法和仿写建议：

| 章节 | 向量分 | 返回的技法 |
|---|---:|---|
| 第0833章 李乐平的电话 | 0.6017 | 动作停顿与对话并置 |
| 第1032章 信使间的会面 | 0.5898 | 内外反差与家庭羁绊 |
| 第1027章 异动 | 0.5946 | 生理细节与心理独白 |

这些结果提供了停顿、内外反差及生理反应等可借鉴的写法，但并不完整覆盖“深夜独自收到坏消息、想哭却哭不出、反复深呼吸”的所有条件。本例支持把快速模式作为获取初步参考的入口，不能仅凭返回三段认定其质量优于其他模式。

### 4.2 精细检索

本地判断和主 LLM 判断两份结果均为 `rounds=1`、`confirmed=0`、`count=0`。可以确认：仅切换判断模型，没有让该次原始需求获得确认片段。

旧记录描述当时使用情绪分类评分、0～5 分及阈值 3，并记载前五段诊断评分均为零。`judge_parts.json`（本地原始材料）保留了诊断提示词，`judge_diag.py`（本地原始材料）保留了诊断脚本，但未见独立的逐项诊断响应存档。因此，“全部候选均低于阈值”等细节属于历史记录，不能从空结果 JSON 单独推导。

可能因素包括候选池与需求不匹配、评分标准偏严格，以及判断输入截断为 400 字。两种判断模型都返回空结果，并不能排除提示词、截断、解析或召回流程问题，也不足以证明“不是 bug”。

### 4.3 深度迭代

保留的 `retry_count=1` 样例运行两轮，原查询和改写查询均未确认片段。

`retry_count=2` 的完整历史如下：

| 轮次 | 新确认段数 | 裁判是否认为改进 | 保留的最佳章节 |
|---|---:|---|---|
| 1 | 0 | 否 | 无 |
| 2 | 3 | 是，建立基准 | 第0864章 迟来的短信 |
| 3 | 3 | 否 | 第0864章 迟来的短信 |
| 4 | 1 | 否 | 第0864章 迟来的短信 |
| 5 | 3 | 否 | 第0864章 迟来的短信 |

新增数量合计为 10，与 `confirmed=10` 一致。第二轮改写后首次找到确认片段，说明本次查询改写改变了检索结果；后续三轮虽继续增加候选，裁判并未认为质量提高。

最终精选输出如下：

| 章节 | 向量分 | LLM 判断分 | 返回的技法 |
|---|---:|---:|---|
| 第0024章 登录网站 | 0.4463 | 5 | 动作与心理穿插写压抑 |
| 第1074章 夜话 | 0.4866 | 5 | 留白与内心独白 |
| 第0853章 意识的侵蚀 | 0.4608 | 5 | 直白心理剖白 |

需要区分“历史最佳章节”和“最终精选输出”：本次历史最佳《迟来的短信》不在最终三段中。裁判还多次指出候选未完整贴合原需求，因此不能将判断满分或确认数量当作整体质量保证。`retry_count=2` 只保留一次成功运行，尚不足以得出“稳定出结果”的结论。

## 5. 补充验证：场景化需求

`query_scene.json`（本地原始材料）中的需求为：

> 深夜独处的出租屋里，他收到一条短信，读完整个人僵在原地，喉头发紧，想哭却哭不出来，只能一遍遍深呼吸强压翻涌的情绪

`补充测试脚本`（本地原始材料）沿用召回 60、最多两轮、每批五段、输出三段、截断 400 字及情绪分类参数。结果为实际两轮、确认三段、输出三段：

| 章节 | 向量分 | LLM 判断分 | 返回的技法 |
|---|---:|---:|---|
| 第0864章 迟来的短信 | 0.5554 | 5 | 生理描写+环境烘托 |
| 第0690章 涌来的凶险 | 0.5331 | 4 | 心理与动作描写 |
| 第0823章 信纸 | 0.5395 | 5 | 对话与场景描写 |

此处按 JSON 将旧记录的“第0823章 报纸”更正为“第0823章 信纸”。

场景化需求增加了出租屋、短信、喉头发紧等多项细节，并非只改了一个词。本次从零输出变成三段输出，支持优先尝试具体化查询；由于只有一组对照，不能量化普遍提升幅度，也不能把全部改善归因于某一个新增要素。

## 6. 结论与使用建议

1. **快速模式适合先找参考。** 本例一次返回三段技法材料；仍需人工判断可迁移性及与原需求的贴合度。
2. **精细模式可能正常完成但无结果。** 该案例中更换判断模型未解决问题，而场景化改写后获得结果。遇到空结果可先检查查询、召回候选和截断范围。
3. **深度改写能探索不同候选。** 本次第五轮完成时确认十段，但第二轮后裁判没有认可进一步改进；更多轮数不必然带来更好结果。
4. **先改查询，再增加迭代成本。** 原始需求的深度成功记录约 205 秒，场景化后的精细记录为 72.4 秒；这是两个不同查询条件下的案例，不是严格的模式速度排名。
5. **暂不宣称稳定性、准确率或成本节省比例。** 缺少重复采样、人工标注和 token 用量。旧记录中的调用次数仅供回顾，不能作为实测账单或现版本费用估算。

## 7. 与当前版本的区别

当前 README 描述了 BGE-M3 dense/sparse/ColBERT 混合精排、统一参考价值判断及可选的 AI 查询优化。当前精细实现还使用弱相关软种子进行反馈，确认阈值为 2；旧测试则携带 `category="emotion"`，旧记录描述阈值为 3。

现有历史结果没有保存混合精排是否启用、模型冷启动状态、代码版本及完整运行配置，不能把本报告直接用作当前混合精排链路的性能证明。当前行为及调参以[使用指南](使用指南.md)和[设计方案](设计方案.md)为准。

## 8. 当前版本的后续验证

历史脚本和测试用小说索引未随公开仓库发布，不能从本仓库直接复跑同一历史实验。可先使用 [公开演示素材](examples/README.md)体验当前版本。

若要评价当前版本，建议使用有权使用的固定范本，固定模型和配置，每种模式在相同需求下重复运行，补充多种场景及人工评价；保存代码版本、混合精排实际启用状态、冷/热启动、完整耗时、请求次数及 token 用量，再统计成功率、耗时分布和质量差异。

## 9. 公开证据与回归验证

- [历史指标摘要](evaluation/historical-metrics.json)：六份本地结果的状态（如有）、查询、轮数、确认数、输出数、章节、分数、技法名称及深度迭代数量记录。保留源文件 SHA-256 以标识原件；未保留小说正文、匹配引文、建议及包含引文的裁判理由。
- 耗时与环境仍仅来自历史文字记录，不是从 JSON 中测得，也不是本次复测结果。
- [公开版验证记录](公开版验证记录.md)：当前公开副本的代码回归结果，与本历史检索案例分别记录。
